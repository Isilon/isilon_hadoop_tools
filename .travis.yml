language: python
python:
  - '2.7'
  - '3.5'
  - '3.6'
before_install: pip install --upgrade pip setuptools wheel
install: pip install --upgrade tox-travis
script:
  - tox -- -r a
  - if [ "$TRAVIS_PYTHON_VERSION" = '3.5' ]; then tox -e style; fi
